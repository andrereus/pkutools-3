# Android bundle (TWA)

Settings for **PWABuilder → Package for stores → Android → All settings**. Everything not listed keeps its default.

- Increment the version.
- Include the source code.
- Sign with the existing signing key.

Icon URL is `/pwa-512x512.png`, the rounded tile. It is also the splash icon, shown unmasked and centered on the background color.

## Colors

Theme and navigation match the app surfaces (`bg-gray-50 dark:bg-gray-950`); the splash keeps the brand sky.

| Setting | Value |
| --- | --- |
| Theme color | `#f9fafb` |
| Theme dark color | `#030712` |
| Background color (splash) | `#0ea5e9` |
| Nav color | `#f9fafb` |
| Nav dark color | `#030712` |
| Nav divider color | `#f9fafb` |
| Nav divider dark color | `#030712` |
